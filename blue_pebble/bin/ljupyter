#!/bin/env bash

while [[ $# -gt 0 ]]; do
  key="$1"
  case $key in
    --port)
      port="$2"
      ;;
    *) # other options assumed to be for lscript
      ;;
  esac
done

lsub_nonblock $@ -- jupyter notebook --port ${port} --no-browser
